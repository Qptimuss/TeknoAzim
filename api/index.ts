import { createServer } from '../server';

// Bu dosya, Vercel'in sunucusuz fonksiyonları için bir giriş noktası görevi görür.
// Mevcut Express uygulamanızı içe aktarır ve dışa aktarır.
export default createServer();